import { Card, styled } from "@mui/material";

export const StyledListHeader = styled(Card)`
        display: flex;
        padding: 0.5rem;
        min-height: 2rem;
        align-items: center;
        justify-content: space-between;
`